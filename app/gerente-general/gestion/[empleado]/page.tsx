"use client"
//import { useEffect, useState } from "react";
// import { obtenerEmpleado } from "../../api/routes";


export default function Empleado({empleado}: { empleado: number }) {
    /*
        useEffect(() => {
            const fetchEmpleados = async () => {
                try {
                    const response = await consultarempleados(
                        { roles: ['admin', 'analyst', 'supervisor'] },
                        localStorage.getItem('token') || ''
                    );
    
                    const empleadosData = Array.isArray(response?.data) ? response.data : [];
                    setEmpleados(empleadosData);
                } catch (error) {
                    console.error('Error cargando empleados:', error);
                    setEmpleados([]);
                } finally {
                    setLoading(false);
                }
            };
    
            fetchEmpleados();
        }, []);*/
    
}